import GsapAnimateFromDirective from './gsap-animate-from.directive';

describe('GsapAnimateFromDirective', () => {
  it('should create an instance', () => {
    const directive = new GsapAnimateFromDirective();
    expect(directive).toBeTruthy();
  });
});
