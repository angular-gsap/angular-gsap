import GsapAnimateFromToDirective from './gsap-animate-from-to.directive';

describe('GsapAnimateFromToDirective', () => {
  it('should create an instance', () => {
    const directive = new GsapAnimateFromToDirective();
    expect(directive).toBeTruthy();
  });
});
