import GsapAnimateDirective from './gsap-animate.directive';

describe('GsapAnimateDirective', () => {
  it('should create an instance', () => {
    const directive = new GsapAnimateDirective();
    expect(directive).toBeTruthy();
  });
});
