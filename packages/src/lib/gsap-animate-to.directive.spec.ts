import GsapAnimateToDirective from './gsap-animate-to.directive';

describe('GsapAnimateToDirective', () => {
  it('should create an instance', () => {
    const directive = new GsapAnimateToDirective();
    expect(directive).toBeTruthy();
  });
});
